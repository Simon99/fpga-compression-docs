---
layout: default
title: 實驗報告
---

# FPGA 壓縮/加密算法實驗報告

**日期:** 2026-02-14  
**版本:** v1.0

---

## 1. 實驗目標

評估不同壓縮演算法在 FPGA 實現上的可行性，重點考量：
- 內存需求（FPGA BRAM 資源有限）
- 算法複雜度（狀態機實現難度）
- 壓縮率（實際效果）

---

## 2. 測試環境

### 代碼庫

| 庫 | 用途 |
|----|------|
| [zlib](https://github.com/madler/zlib) | gzip/DEFLATE 官方實現 |
| [tiny-AES-c](https://github.com/kokke/tiny-AES-c) | AES 極簡 C 實現 |
| [mbedtls](https://github.com/Mbed-TLS/mbedtls) | RSA 工業級實現 |

### 測試數據
- **來源:** FASTQ 基因測序數據
- **測試檔案:** 1.1GB ~ 4.5GB FASTQ 文件

---

## 3. 壓縮測試結果

### 3.1 主要對比 (test1.fastq, 1122.9 MB)

| 演算法/模式 | 壓縮後 | 壓縮率 | 時間 | 內存需求 | FPGA 適用性 |
|------------|--------|-------|------|---------|------------|
| **gzip -1 (fast)** | 236.6 MB | 78.9% | 7.6s | ~32KB | ⚠️ 中等 |
| **gzip -9 (best)** | 160.2 MB | 85.7% | 2m20s | ~32KB | ⚠️ 中等 |
| **gzip wbits=9** | 347.1 MB | 69.1% | 27s | **~512B** | ✅ 最佳 |
| xz (LZMA2) | 91.0 MB | 91.9% | - | >1MB | ❌ 不適用 |
| zstd | 106.0 MB | 90.6% | - | >128KB | ❌ 不適用 |

### 3.2 視覺化對比

```
原始:     ████████████████████████████████████████ 1122.9 MB (100%)

gzip -1:  █████████ 236.6 MB (21.1%)
gzip -9:  ██████ 160.2 MB (14.3%)
wbits=9:  ████████████ 347.1 MB (30.9%)
xz:       ███ 91.0 MB (8.1%)
zstd:     ████ 106.0 MB (9.4%)
```

### 3.3 多檔案驗證

| 檔案 | 原始大小 | gzip -1 | gzip -9 | 壓縮率差 |
|------|---------|---------|---------|---------|
| test1.fastq | 1.1 GB | 236.6 MB | 160.2 MB | 32.3% |
| test2.fastq | 1.1 GB | 251.9 MB | 173.4 MB | 31.2% |
| test3.fastq | 4.3 GB | 839.7 MB | 586.5 MB | 30.2% |

---

## 4. 關鍵發現

### 壓縮效率
- **-9 vs -1:** 體積減少 ~32%，但時間增加 **18 倍**
- 對於基因數據，gzip 壓縮率穩定在 78-86%

### FPGA 實現考量

#### ✅ wbits=9 模式最適合 FPGA
- 滑動窗口僅需 512 bytes (vs 32KB)
- 可用 Block RAM 輕鬆實現
- 壓縮率雖降至 69%，但仍有實用價值

#### 📐 算法複雜度
- DEFLATE = LZ77 + Huffman
- 狀態機約 10 個狀態
- 可參考 zlib/deflate.c 的結構

#### ❌ 不建議 FPGA 實現的算法
- **xz/LZMA:** 字典過大，狀態機複雜
- **zstd:** 需要大量查表，內存消耗高

---

## 5. 代碼分析準備

### zlib 核心檔案
```
zlib/
├── deflate.c    # 壓縮核心 (DEFLATE 狀態機)
├── inflate.c    # 解壓核心
├── trees.c      # Huffman 編碼
└── adler32.c    # 校驗和
```

### tiny-AES-c 結構
```
tiny-AES-c/
├── aes.c        # AES 核心 (~200 行)
└── aes.h        # 支援 AES-128/192/256
```

---

## 6. 下一步建議

1. **DEFLATE 狀態機分析** — 詳細分析 deflate.c 的狀態轉換
2. **Verilog 原型** — 先實現 wbits=9 版本的 LZ77
3. **AES 硬件化** — tiny-AES-c 結構簡單，適合直接轉換
4. **RSA 評估** — mbedtls 的 RSA 較複雜，需評估是否適合 FPGA

---

## 附錄：測試命令

```bash
# 快速壓縮
gzip -1 -k test1.fastq

# 最佳壓縮
gzip -9 -k test1.fastq

# 低內存模式 (Python)
import zlib
compressor = zlib.compressobj(level=6, wbits=9)

# xz 壓縮
xz -k test1.fastq

# zstd 壓縮
zstd test1.fastq
```

---

[← 返回首頁](index.html)

*報告生成: 2026-02-14 | 版本: v1.0*
